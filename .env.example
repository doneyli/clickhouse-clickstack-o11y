# Anthropic API Key (required for AI dashboard builder)
ANTHROPIC_API_KEY=

# HyperDX API Key (auto-retrieved from MongoDB during setup)
# Retrieve manually with:
#   docker exec hyperdx-local mongo --quiet --eval \
#     'db=db.getSiblingDB("hyperdx"); print(db.users.findOne({}).accessKey)'
HYPERDX_API_KEY=

# HyperDX endpoints
HYPERDX_API_URL=http://localhost:8000
HYPERDX_UI_URL=http://localhost:8080
OTLP_ENDPOINT=http://localhost:4318
CLICKHOUSE_URL=http://localhost:8123
